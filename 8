#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>
#include <algorithm>
using namespace std;
int main(){
    int t;
    cin >> t;
    int f[100];
    f[0]=0;
    f[1]=1;
    for (int i=2;i<100;i++){
    	f[i]=f[i-1]+f[i-2];
	}
    while (t--){
    	int n;
    	cin >> n;
    	int a[n+1];
    	for (int i=0;i<n;i++) cin >> a[i];
    	for (int i=0;i<n;i++){
    		int index=0;
    		for (int j=0;j<100;j++){
    			if (a[i]<=f[j]){
    				index=j;
    				break;
				}
			}
			if (a[i]==f[index]) cout << a[i] << " ";
		}
    	cout << "\n" ;
	}
}
